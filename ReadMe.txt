annotation basically is metadata and to provide additional information to JVM or java compiler we use java annotations.

Note:-
Output you see is generated from initialized objects from Model Package during DFS traversal and initialize of objects in Singleton java class.